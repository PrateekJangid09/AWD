'use client'; import { FormEvent, useState } from 'react'; import Link from 'next/link';
export default function ActionGrid() { const [email, setEmail] = useState(''); const [status, setStatus] = useState(''); const [busy, setBusy] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); setStatus(''); try { const response = await fetch('/api/newsletter', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ email }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setStatus('Subscribed successfully.'); setEmail(''); } catch (error) { setStatus(error instanceof Error ? error.message : 'Unable to subscribe.'); } finally { setBusy(false); } } return <div className="bg-[#050505] px-5 py-24 text-white"><div className="mx-auto grid max-w-6xl gap-px bg-white/15 md:grid-cols-2"><section className="bg-[#050505] p-10"><h2 className="text-3xl font-bold">The Monday Dispatch</h2><p className="mt-3 text-white/60">A weekly collection of website design references. No inflated audience claims.</p><form className="mt-7 flex flex-wrap gap-3" onSubmit={submit}><input className="min-w-56 flex-1 rounded-lg bg-white/10 px-4 py-3" type="email" required value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" /><button disabled={busy} className="rounded-lg bg-[#CCFF00] px-6 py-3 font-semibold text-black">{busy ? 'Sending…' : 'Subscribe'}</button></form><p role="status" className="mt-3 text-sm">{status}</p></section><section className="grid gap-px bg-white/15"><Link className="bg-[#050505] p-10" href="/submit"><h2 className="text-2xl font-bold">Submit a site</h2><p className="mt-2 text-white/60">Send your work for editorial review.</p></Link><Link className="bg-[#050505] p-10" href="/research/website-design-index-2026"><h2 className="text-2xl font-bold">Read the design index</h2><p className="mt-2 text-white/60">See the catalogue’s category composition and methodology.</p></Link></section></div></div>; }
