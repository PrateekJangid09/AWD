# WebPalette Studio v1.2 — UI Polish

## Palette cards
- Removed status pills and the floating × from the color swatch.
- Role labels are now clean typography directly on the color surface.
- Moved source/generated/chosen/role-lock state into a quiet decision line under the HEX value.
- Replaced the floating × with a restrained trash action in the card body.
- Converted Edit / Why this role? from boxed controls into text actions.
- Reduced visual weight of reorder controls.
- Allowed long color names to wrap to two lines instead of hard truncating immediately.
- Reworked the palette decision summary from pills into a simple separated status line.

## Readiness score
- Increased ring size and inner breathing room.
- Stacked the score and `out of 100` vertically so `100` never collides with the ring.
- Converted readiness metadata pills into a simple dot-separated line.
- Preserved the reward halo and completion state.
