import type { Metadata } from 'next'; import Link from 'next/link'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; import { getWebsites } from '@/lib/data'; import { getCategoryColor, slugifyCategory } from '@/lib/categories'; import { AUDIT_REVIEW_DATE, SITE_URL } from '@/lib/site';
export const revalidate = 3600;
export const metadata: Metadata = { title: '2026 Website Design Index: Catalogue Composition', description: 'A reproducible snapshot of the categories represented in the AllWebsites.Design editorial catalogue.', alternates: { canonical: '/research/website-design-index-2026' } };
export default async function IndexPage() { const websites = await getWebsites(); const counts = new Map<string, number>(); websites.forEach((website) => { const category = website.displayCategory || website.category; counts.set(category, (counts.get(category) || 0) + 1); }); const rows = [...counts].map(([category, count]) => ({ category, count, share: websites.length ? count / websites.length * 100 : 0 })).sort((a, b) => b.count - a.count); const schema = { '@context': 'https://schema.org', '@type': 'Dataset', name: '2026 AllWebsites.Design Catalogue Composition', url: `${SITE_URL}/research/website-design-index-2026`, dateModified: AUDIT_REVIEW_DATE, measurementTechnique: 'Counts of cleaned, deduplicated public catalogue records grouped by governed category.' }; return <><Header /><main className="min-h-screen bg-[#f6f3ea] px-5 pb-24 pt-32 text-black"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} /><article className="mx-auto max-w-5xl"><p className="text-sm uppercase tracking-widest">Research note · reviewed {AUDIT_REVIEW_DATE}</p><h1 className="mt-4 text-5xl font-black md:text-8xl">2026 Website Design Index</h1><p className="mt-8 max-w-3xl text-xl leading-8 text-neutral-700">A transparent snapshot of {websites.length.toLocaleString()} cleaned and deduplicated website records.</p><div className="mt-14 overflow-x-auto border-y border-black"><table className="w-full"><thead><tr className="border-b border-black text-left"><th className="py-4">Category</th><th className="text-right">Examples</th><th className="text-right">Share</th></tr></thead><tbody>{rows.map((row) => <tr className="border-b border-black/15" key={row.category}><td className="py-4 font-semibold"><span className="mr-3 inline-block h-3 w-3 rounded-full" style={{ backgroundColor: getCategoryColor(row.category) }} /><Link className="underline underline-offset-4" href={`/c/${slugifyCategory(row.category)}`}>{row.category}</Link></td><td className="text-right">{row.count.toLocaleString()}</td><td className="text-right">{row.share.toFixed(1)}%</td></tr>)}</tbody></table></div><section className="mt-14 max-w-3xl"><h2 className="text-3xl font-bold">Methodology and limits</h2><p className="mt-5 text-lg leading-8">Counts exclude hidden, invalid, placeholder, and duplicate records. Categories use a governed automated mapping with explicit reviewed corrections. This describes catalogue composition, not the entire web, and automated classifications should be manually reviewed before use as factual research.</p></section></article></main><Footer /></>; }
