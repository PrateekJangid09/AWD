{
  "name": "allwebsites-design-engine",
  "version": "0.1.0",
  "description": "Design-directory intelligence: per-datapoint micro-algorithms that extract name, description, category/subcategory/type/audience, key pages, palette, fonts, style, socials and a precise full-page screenshot.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node scripts/seed.js",
    "export": "node scripts/export-public.js",
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "express": "^4.22.2"
  },
  "optionalDependencies": {
    "playwright": "^1.45.0"
  }
}
