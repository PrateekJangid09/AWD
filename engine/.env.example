# Required for /admin and /api/admin/*
ADMIN_SECRET=change-me-to-a-long-random-string
SESSION_SECRET=change-me-to-another-long-random-string

# Server bind (local-first defaults)
HOST=127.0.0.1
PORT=3000

# Cookie Secure flag: 0 for plain http://127.0.0.1, 1 behind HTTPS
COOKIE_SECURE=0
